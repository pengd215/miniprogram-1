/* pages/stock/warning.wxss */
/* 1. 全局容器：去除默认边距，背景色设为浅灰以突出卡片 */
page {
  background-color: #f7f8fa;
  height: 100%;
}

.container {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

/* 2. 卡片样式：实现“宽覆盖全屏”的效果 */
.card {
  background: #ffffff;
  /* 关键：左右 margin 设为 0，实现全宽 */
  width: 90%; 
  box-sizing: border-box;
  padding: 50rpx 40rpx 60rpx 40rpx;
  
  /* 视觉优化：只保留底部圆角，像一张大卡片铺下来 */
  border-radius: 0 0 40rpx 40rpx; 
  /* 柔和的投影，增加层次感 */
  box-shadow: 0 10rpx 30rpx rgba(0, 0, 0, 0.05);
  /* 如果内容很多，允许卡片向下延伸 */
  flex: 1; 
}

/* 3. 头部区域 */
.card-header {
  margin-bottom: 60rpx;
}

.card-title {
  font-size: 38rpx;
  font-weight: 700;
  color: #1a1a1a;
  display: block;
  margin-bottom: 16rpx;
  letter-spacing: 1rpx;
}

.tip {
  font-size: 26rpx;
  color: #999999;
  line-height: 1.5;
  display: block;
}

/* 4. 表单区域 */
.form-group {
  margin-bottom: 60rpx;
}

.form-item {
  margin-bottom: 40rpx;
}

.label {
  font-size: 30rpx;
  color: #333333;
  font-weight: 500;
  display: block;
  margin-bottom: 20rpx;
}

/* 输入框美化 */
.input {
  width: 100%;
  height: 96rpx;
  background-color: #f5f6f8;
  border-radius: 16rpx;
  padding: 0 24rpx;
  box-sizing: border-box;
  font-size: 32rpx;
  color: #333;
  border: 2rpx solid transparent; /* 预留边框位置防止抖动 */
  transition: all 0.3s ease;
}

/* 输入框聚焦时的效果 */
.input:focus {
  background-color: #fff;
  border-color: #3a701f;
  box-shadow: 0 0 0 6rpx rgba(63, 143, 24, 0.1);
}

.desc {
  font-size: 24rpx;
  color: #b0b0b0;
  display: block;
  margin-top: 12rpx;
  padding-left: 4rpx;
}

/* 5. 按钮美化：文字居中与交互 */
.save-btn {
  width: 100%;
  height: 96rpx;
  line-height: 96rpx; /* 垂直居中 */
  background: linear-gradient(135deg, #4c6baf 0%, #2646af 100%); /* 渐变色 */
  color: #fff;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 48rpx; /* 胶囊形状 */
  border: none;
  margin-top: 20rpx;
  
  /* 关键：Flex布局确保文字绝对居中 */
  display: flex;
  align-items: center;
  justify-content: center;
  
  box-shadow: 0 10rpx 20rpx rgba(63, 143, 24, 0.3);
  letter-spacing: 2rpx;
}

/* 按钮点击态 */
.btn-hover {
  opacity: 0.9;
  transform: scale(0.98);
}

/* 移除小程序 button 默认的边框 */
.save-btn::after {
  border: none;
}